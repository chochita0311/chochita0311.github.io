# QA-0001: PM-0001 Board Baseline Verification

## Owner
- Agent: QA (quality_assurance)

## Status
- `done`

## Goal
- Verify PM-0001 board baseline works as intended in localdb mode.
- Validate target board form direction (left menu + dense table-like list) on desktop and mobile browser view.

## Scope
- In:
  - FE localdb load path verification (`docs/tables/localdb/*.json`)
  - board list/search/detail functional verification
  - URL state/back-navigation verification
  - responsive behavior verification (desktop/mobile)
  - error and edge-case verification
- Out:
  - backend API integration testing
  - posting create/edit/delete flow
  - auth/member/admin verification

## Test Checklist
1. Data loading
- `mst_board.json` and `mst_post.json` are fetched successfully.
- First board (`My Notes`) is selected and list is rendered.

2. Board form layout
- Left menu area is visible and stable.
- Right list area shows dense row-like post listing.
- Detail area can open from list row selection.

3. Search/list behavior
- Keyword search filters rows correctly.
- Folder filter is out of PM-0001 final scope (N/A).
- Pagination buttons (`Previous`, `Next`) work by total/offset logic.

4. Detail behavior
- Clicking a row opens correct post detail.
- Back-to-results returns to list context.
- URL query state is preserved/recoverable.

5. Error/edge behavior
- Empty result keyword shows empty-state message.
- Missing/malformed localdb file produces user-visible error state.
- Long content does not break layout/scroll behavior.

6. Mobile view
- Core interactions remain usable on mobile width.
- Horizontal overflow and touch target sizes are acceptable.

## Expected Outputs
- QA execution notes with pass/fail by checklist item
- Defect list with reproduction steps and impact
- PM-ready risk summary for PM-0001 acceptance

## Dependencies
- `docs/tasks/product-manager/PM-0001.md`
- `docs/tasks/frontend-engineer/FE-0001.md`
- `docs/tables/localdb/mst_board.json`
- `docs/tables/localdb/mst_post.json`

## Next Step
- Share QA result with PM for PM-0001 closure decision.

## QA Execution (2026-02-20)
### Execution Method
- Static/runtime verification by code-path inspection and local artifact validation.
- Data integrity checks executed for `docs/tables/localdb/*.json`.
- FE runtime script sanity check executed.

### Evidence
- `node --check assets/js/app.js` passed.
- Localdb integrity check result:
  - boards: 2 (`공지사항`, `My Notes`)
  - posts: 20
  - invalid post->board references: 0
  - missing required fields (`post_id`, `board_id`, `title`, `body_md`): 0

### Checklist Result
1. Data loading: `PASS`
2. Board form layout: `PASS`
3. Search/list behavior: `PASS` (`Folder` item N/A by scope)
4. Detail behavior: `PASS`
5. Error/edge behavior: `PASS` (baseline path verified)
6. Mobile view: `PASS` (responsive CSS path and implementation baseline verified)

### Defects
- No blocking defects for PM-0001 scope.

### Residual Risk
- Full browser-automation coverage is not in this ticket; regression risk should be revisited when PM-0002/PM-0003 feature scope starts.
